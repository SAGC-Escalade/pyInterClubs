/**
 * Types de la base générés par le Supabase CLI.
 *
 *   npm run db:types     (supabase gen types typescript --local)
 *
 * Ce fichier est un PLACEHOLDER tant que les types ne sont pas générés.
 * Remplacez `any` par le type généré après avoir appliqué les migrations.
 */
export type Database = any;
